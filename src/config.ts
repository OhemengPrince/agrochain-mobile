// Toggle this to false once the real backend is ready — every api/* module
// branches on this flag to decide between mock data and live HTTP calls.
export const USE_MOCK_DATA = true;

export const MOCK_DELAY_MS = 500;
